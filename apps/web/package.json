{
  "name": "@fintrack/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b && vite build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint . --ext ts,tsx"
  }
}
